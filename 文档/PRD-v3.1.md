**AI 漫剧分镜脚本助手**
- [1. 产品概述](#1-产品概述)
  - [1.1 产品定位](#11-产品定位)
  - [1.2 技术架构](#12-技术架构)
- [2. 设计规范（灵镜 Design System v1.0）](#2-设计规范灵镜-design-system-v10)
  - [2.1 颜色 Colors](#21-颜色-colors)
    - [主色与基础色](#主色与基础色)
    - [语义色](#语义色)
  - [2.2 字体 Typography](#22-字体-typography)
  - [2.3 字号 Font Sizes](#23-字号-font-sizes)
  - [2.4 圆角 Radius](#24-圆角-radius)
  - [2.5 阴影 Shadows](#25-阴影-shadows)
  - [2.6 间距 Spacing](#26-间距-spacing)
  - [2.7 组件 Components](#27-组件-components)
  - [2.8 图标规范](#28-图标规范)
  - [2.9 iOS 体验优化](#29-ios-体验优化)
- [3. App 结构与页面（详细 UI 组件）](#3-app-结构与页面详细-ui-组件)
  - [3.1 页面导航（Screen 切换）](#31-页面导航screen-切换)
  - [3.2 登录页（未实现）](#32-登录页未实现)
  - [3.3 创作页（首页）— 详细组件](#33-创作页首页-详细组件)
    - [区域 A：用户问候栏](#区域-a用户问候栏)
    - [区域 B：探索灵感](#区域-b探索灵感)
    - [区域 C：故事输入](#区域-c故事输入)
  - [3.4 引导式创作流程（3 步）](#34-引导式创作流程3-步)
    - [步骤 1/3 — 选择时长](#步骤-13--选择时长)
    - [步骤 2/3 — 选择风格](#步骤-23--选择风格)
    - [步骤 3/3 — 选择镜头数 + 图片比例](#步骤-33--选择镜头数--图片比例)
  - [3.5 加载页与后台任务系统](#35-加载页与后台任务系统)
    - [加载页](#加载页)
    - [后台任务系统](#后台任务系统)
  - [3.6 脚本结果页（只读展示）](#36-脚本结果页只读展示)
  - [3.7 生图过程（未实现）](#37-生图过程未实现)
  - [3.8 生图完成后（未实现）](#38-生图完成后未实现)
  - [3.9 导出（未实现）](#39-导出未实现)
  - [3.9 探索页 — 详细组件](#39-探索页--详细组件)
    - [区域 A：页面标题](#区域-a页面标题)
    - [区域 B：搜索栏](#区域-b搜索栏)
    - [区域 C：风格筛选标签](#区域-c风格筛选标签)
    - [区域 D：作品瀑布流](#区域-d作品瀑布流)
  - [3.10 我的页面 — 详细组件](#310-我的页面--详细组件)
    - [区域 A：页面标题栏](#区域-a页面标题栏)
    - [区域 B：个人信息卡片](#区域-b个人信息卡片)
    - [区域 C：数据统计栏](#区域-c数据统计栏)
    - [区域 D：功能菜单列表](#区域-d功能菜单列表)
- [4. 用户操作流程](#4-用户操作流程)
  - [4.1 核心流程：创作漫画](#41-核心流程创作漫画)
  - [4.2 探索灵感流程](#42-探索灵感流程)
  - [4.3 探索发现流程](#43-探索发现流程)
  - [4.4 个人中心流程](#44-个人中心流程)
- [5. 前后端技术交互流程](#5-前后端技术交互流程)
  - [5.1 创作流程交互](#51-创作流程交互)
  - [5.2 单格操作交互](#52-单格操作交互)
  - [5.3 探索页交互（未实现）](#53-探索页交互未实现)
  - [5.4 我的页面交互（未实现）](#54-我的页面交互未实现)
  - [5.5 自动保存草稿（未实现）](#55-自动保存草稿未实现)
- [6. 脚本生成规范](#6-脚本生成规范)
  - [6.1 叙事黄金公式](#61-叙事黄金公式)
  - [6.2 禁止项](#62-禁止项)
  - [6.3 镜头数与叙事结构映射](#63-镜头数与叙事结构映射)
  - [6.4 8 种脚本风格](#64-8-种脚本风格)
  - [6.5 时长选项](#65-时长选项)
  - [6.6 图片比例](#66-图片比例)
  - [6.7 缓存规则](#67-缓存规则)
- [7. 后端服务设计](#7-后端服务设计)
  - [7.1 LangGraph 状态图](#71-langgraph-状态图)
    - [链路 1：生成脚本（POST /api/generate-script）](#链路-1生成脚本post-apigenerate-script)
    - [链路 2：生成图片（POST /api/generate-images，SSE）](#链路-2生成图片post-apigenerate-imagessse)
    - [链路 3：单格操作（直接调用，不经过 LangGraph）](#链路-3单格操作直接调用不经过-langgraph)
  - [7.2 API 接口完整清单](#72-api-接口完整清单)
    - [MVP（12 个接口，已实现 4 个）](#mvp12-个接口已实现-4-个)
    - [V2（13 个接口，均未实现）](#v213-个接口均未实现)
    - [V3（3 个接口，均未实现）](#v33-个接口均未实现)
- [8. 环境变量配置](#8-环境变量配置)
- [9. 开发路线图](#9-开发路线图)
  - [9.1 第 1 周：后端核心 + 脚本生成 — **已完成**](#91-第-1-周后端核心--脚本生成--已完成)
  - [9.2 第 2 周：生图 + 存储 — **部分完成**](#92-第-2-周生图--存储--部分完成)
  - [9.3 第 3 周：用户系统 + 数据库 — **未开始**](#93-第-3-周用户系统--数据库--未开始)
  - [9.4 第 4 周：React Native 前端 — **部分完成**](#94-第-4-周react-native-前端--部分完成)
  - [9.5 第 5 周：联调 + 优化 + 上架 — **未开始**](#95-第-5-周联调--优化--上架--未开始)
- [10. 当前实现状态总览](#10-当前实现状态总览)
- [11. 确认汇总表](#11-确认汇总表)


*Comic Storyboard AI Assistant*

产品需求文档 PRD

v3.1

2026年3月29日

> v3.1 更新说明：基于已实现代码（前端 App.tsx + 后端 FastAPI）对照 v3.0，修正了实际实现与文档的差异。新增「当前实现状态」章节，标注每个功能的完成度。引导式创作流程由 4 步改为 3 步（文案输入已合并到首页），底部 Tab 栏改为无 TabBar 的 screen 切换，新增后台任务系统说明。

---

# 1. 产品概述

## 1.1 产品定位

AI 漫剧分镜脚本助手是一款面向漫剧创作小白的 AI 工具。用户只需输入一句简单文案，AI 自动完成故事拆解、分镜脚本生成、漫画图片生成的全流程。产品同时支持 iOS App 和网页版。

## 1.2 技术架构

| **层级** | **技术选择** | **说明** |
| --- | --- | --- |
| 前端 | React Native | 一套代码同时出 iOS App + 网页版 + 未来 Android |
| 后端 | Python FastAPI | API 服务，REST + SSE |
| AI 编排 | LangGraph | 状态图编排多步骤 AI 工作流 |
| 文本 AI | 通义千问 qwen-plus | 故事拆解、脚本生成 |
| 生图 AI | 通义万相 wan2.5-t2i + Nano Banana | 漫画图片生成 |
| 数据库 | MySQL | 用户、作品、积分等 |
| 图片存储 | 阿里云 OSS | 生成的漫画图片 |
| 部署 | 阿里云 ECS | 2核4G，MVP 阶段 |
| 开发工具 | Claude Code + Pencil MCP | 代码生成 + 设计稿读取 |

---

# 2. 设计规范（灵镜 Design System v1.0）

## 2.1 颜色 Colors

### 主色与基础色

| **Token** | **色值** | **用途** |
| --- | --- | --- |
| Primary | #5F33E1 | 主按钮、选中态、强调色 |
| Primary BG | #EBE5FD | 主色浅底、标签背景 |
| Foreground | #1A1A1A | 正文文字、标题 |
| Muted | #717171 | 辅助文字、placeholder |
| Grey BG | #F7F7F7 | 输入框背景、页面底色 |
| Border | #EBEBEB | 分割线、边框 |

### 语义色

| **Token** | **色值** | **背景色** | **用途** |
| --- | --- | --- | --- |
| Error | #FF3B30 | #FFEDEB | 错误提示、删除 |
| Success | #34C759 | #E8F9ED | 成功提示、完成 |
| Warning | #FF9500 | #FFF3E0 | 警告提示 |
| Info | #007AFF | #E5F1FF | 信息提示 |

## 2.2 字体 Typography

| **场景** | **字体** | **字号** | **字重** |
| --- | --- | --- | --- |
| 页面大标题 | Inter | 28px | Bold 700 |
| 用户名·区块标题 | Inter | 22px | Bold 700 |
| 模块标题·卡片标题 | Inter | 17px | Semibold 600 |
| 正文内容·段落文字 | Inter | 15px | Regular 400 |
| 辅助说明·标签文字 | Inter | 13px | Regular 400 |

## 2.3 字号 Font Sizes

| **Token** | **大小** |
| --- | --- |
| xs | 11px |
| sm | 13px |
| md | 15px |
| lg | 17px |
| xl | 20px |
| 2xl | 24px |
| 3xl | 28px |
| title | 34px |

## 2.4 圆角 Radius

| **Token** | **大小** | **适用** |
| --- | --- | --- |
| SM | 8px | 小标签、小按钮 |
| M | 12px | 卡片、输入框 |
| LG | 16px | 大卡片、弹窗 |
| XL | 24px | 大圆角容器 |
| Pill | 100px | 胶囊按钮、Tab 选项 |

## 2.5 阴影 Shadows

| **Token** | **参数** | **色值** | **适用** |
| --- | --- | --- | --- |
| Standard 标准 | y:4 blur:16 | #00000015 | 卡片、输入框、三级按钮、头像等 |
| Elevated 悬浮 | y:8 blur:24 | #00000020 | 弹窗、模态框、悬浮卡片等 |
| Purple 品牌 | y:4 blur:16 | #5F33E130 | 主按钮、CTA 按钮等 |

## 2.6 间距 Spacing

| **Token** | **大小** |
| --- | --- |
| xs | 4px |
| sm | 8px |
| md | 16px |
| lg | 24px |
| xl | 32px |
| 2xl | 48px |

## 2.7 组件 Components

| **组件** | **说明** |
| --- | --- |
| PrimaryButton | 主按钮，背景 Primary #5F33E1，白色文字，圆角 Pill (100px)，品牌阴影 |
| BokehBG | 背景组件，渐变光斑效果 |

## 2.8 图标规范

| **位置** | **图标** | **说明** |
| --- | --- | --- |
| Tab - 创作 | ✨ 星星闪烁 | 选中态 Primary #5F33E1，未选态 Muted #717171 |
| Tab - 探索 | 画笔圆形 | 选中态 Primary #5F33E1，未选态 Muted #717171 |
| Tab - 我的 | 人形轮廓 | 选中态 Primary #5F33E1，未选态 Muted #717171 |
| 顶部通知 | 铃铛 | 创作页右上角，Foreground #1A1A1A |
| 设置 | 齿轮 | 我的页右上角，Foreground #1A1A1A |
| 菜单箭头 | 右箭头 > | 我的页菜单列表每项右侧，Muted #717171 |
| 我的作品 | 图片图标 | Primary #5F33E1 |
| 我的草稿 | 文档图标 | Primary #5F33E1 |
| 我的收藏 | 书签图标 | Primary #5F33E1 |
| 我的点赞 | 爱心图标 | Error #FF3B30 |
| 使用记录 | 时钟图标 | Primary #5F33E1 |

## 2.9 iOS 体验优化

| **特性** | **说明** |
| --- | --- |
| 触感反馈 | 点击按钮轻微振动，生成完成成功振动 |
| 手势操作 | 左滑返回上一页，双指缩放图片，下拉刷新 |
| 转场动画 | 前进：左右滑动+淡入淡出；回退：右滑手势；生成脚本：向上推出 |
| 加载状态 | 动画 + 随机提示词（"AI 正在拆解你的故事..."） |
| 超时处理 | 30 秒超时 + 后端自动重试 1 次，失败显示友好提示 + 重试按钮 |

---

# 3. App 结构与页面（详细 UI 组件）

## 3.1 页面导航（Screen 切换）

> **v3.1 变更**：当前版本无底部 Tab 栏。页面通过 screen state 切换（home / wizard / loading / result / profile / explore）。创作页的头像和探索页的"查看全部"按钮用于跳转。后续版本将恢复底部 Tab 栏。

当前导航方式：

| **页面** | **入口** | **返回方式** |
| --- | --- | --- |
| 创作页（home） | 默认首页 | — |
| 引导式创作（wizard） | 首页点击"生成分镜脚本" | 左上角返回箭头 |
| 加载页（loading） | 引导式创作点击"开始生成" | "返回首页（后台生成）"按钮 |
| 脚本结果页（result） | 加载完成自动跳转 / 完成气泡 | 左上角返回箭头 |
| 探索页（explore） | 首页"查看全部"按钮 | 左上角返回按钮 |
| 我的页面（profile） | 首页头像点击 | 左上角返回按钮 |

## 3.2 登录页（未实现）

> **v3.1 状态**：登录功能尚未实现，当前使用硬编码用户信息（"创作者小明"，ID: 10086）。

- 计划支持手机号、微信、Apple ID 三种登录方式
- 新用户首次登录自动注册

## 3.3 创作页（首页）— 详细组件

创作页为单页纵向滚动布局，从上到下依次排列以下区域：

### 区域 A：用户问候栏

| **组件** | **说明** |
| --- | --- |
| 用户头像 | 左侧，圆形 48px，显示用户头像或默认头像 |
| 问候语 | 头像右侧，"你好！"（14px 灰色）+ 换行 "创作者"（22px 加粗黑色，显示用户昵称） |
| 通知铃铛 | 右上角，铃铛图标，点击进入通知页 |

### 区域 B：探索灵感

| **组件** | **说明** |
| --- | --- |
| 标题 | 左侧 "探索灵感"（20px 加粗） |
| 查看全部 | 右侧 "查看全部"（紫色 #5F33E1，可点击，跳转探索页） |
| 灵感卡片列表 | 横向滚动，每张卡片宽约 160px |

每张灵感卡片结构：

| **元素** | **说明** |
| --- | --- |
| 封面图 | 卡片主体，圆角 12px，高约 200px，占卡片 70% |
| 风格标签 | 封面图左上角浮层，如"悬疑反转""都市奇幻"，半透明黑底白字 |
| 标题 | 封面下方，如"渣男被前女友报复"，16px 白色加粗 |
| 辅助信息 | 标题下方，"3集 · 12.4w观看"，12px 灰色 |

### 区域 C：故事输入

| **组件** | **说明** |
| --- | --- |
| 标题 | "告诉灵镜AI你的故事"（22px 加粗） |
| 副标题 | （当前版本无副标题） |
| 输入框 | 多行文本框，圆角 12px，浅灰背景，placeholder: "输入你的故事，比如：一只猫咪意外获得超能力，拯救了整个城市..." |
| 生成按钮 | 底部固定，宽度撑满（左右留 padding md=16px），高 52px，圆角 Pill (100px)，背景 Primary #5F33E1 + Purple 品牌阴影，白色文字 "生成分镜脚本"，17px Semibold 600 |

## 3.4 引导式创作流程（3 步）

> **v3.1 变更**：原 4 步流程改为 3 步。文案输入已合并到首页区域 C，不再作为独立步骤。进入 Wizard 时自动携带首页输入的文案。

用户点击"生成分镜脚本"按钮后，跳转到引导式创作页面。顶部显示导航栏（步骤 N/3 — 标题），下方圆点进度指示器（●○○），可回退上一步，每步都有默认值。

### 步骤 1/3 — 选择时长

- 胶囊按钮：10秒 / 15秒 / 30秒 / 60秒
- 默认值：15秒
- 说明文字："默认 15 秒，适合标准漫剧节奏"
- ~~自定义~~ 当前版本暂不支持自定义时长

### 步骤 2/3 — 选择风格

- 卡片式网格选择（4列2行），每张卡片带 emoji + 风格名称
- 选中态：紫色背景 + 白色文字 + 右上角勾选标记
- 8 种风格：悬疑反转 / 搞笑沙雕 / 治愈温情 / 暗黑爆爽 / 古风仙侠 / 超能力奇幻 / 恐怖惊悚 / 甜宠恋爱
- 默认值：悬疑反转
- 用户只能选 1 种

### 步骤 3/3 — 选择镜头数 + 图片比例

- 镜头数胶囊按钮：4 / 6 / 9 / 12，默认 6
- 图片比例胶囊按钮：3:4 竖版 / 1:1 方形 / 16:9 横版，默认 3:4
- ~~自定义~~ 当前版本暂不支持自定义比例
- 底部按钮："开始生成"

## 3.5 加载页与后台任务系统

> **v3.1 新增**：这是代码中已实现但 v3.0 未提及的功能。

### 加载页

用户点击"开始生成"后，进入全屏加载页：

| **组件** | **说明** |
| --- | --- |
| 加载动画 | 居中 ActivityIndicator，Primary 色 |
| 标题 | "AI 正在创作中..."（20px 加粗） |
| 副标题 | "正在分析故事结构，拆解分镜脚本\n请稍候片刻"（灰色） |
| 进度步骤 | 4 步竖排列表，带圆点指示（每 2 秒推进一步）：1. 解析故事结构 2. 分配叙事节奏 3. 生成分镜描述 4. 润色与优化 |
| 主按钮 | "返回首页（后台生成）"— 点击后回到首页，AI 继续在后台生成 |
| 次要按钮 | "取消生成"— 取消任务并返回首页 |

### 后台任务系统

生成任务支持后台运行，用户无需停留在加载页等待：

| **组件** | **位置** | **说明** |
| --- | --- | --- |
| 生成中气泡 | 首页顶部（问候栏下方） | 紫色气泡，显示 ActivityIndicator + "AI 创作中：{故事前20字}"，点击回到加载页 |
| 完成气泡 | 首页顶部 | 绿色气泡，"脚本生成完成，点击查看"，点击进入结果页 |
| 待制作内容 | 我的页面菜单 | 显示 badge（"N 生成中" 或 "N 已完成"），点击可查看最近任务 |

任务状态：generating → done / failed

## 3.6 脚本结果页（只读展示）

> **v3.1 变更**：当前版本为只读展示，不支持编辑。AI 重写、单格生图等功能计划在后续版本实现。

已实现：
- 顶部导航栏：左侧返回箭头 + 标题"分镜脚本"
- 信息栏：风格 badge（emoji + 名称）+ "N 格 · 比例"
- 纵向卡片列表，上下滑动
- 每格卡片显示：格号 badge、叙事阶段 badge（Hook/Escalate/Twist/Cliffhanger，颜色编码）、镜头角度、场景描述、角色、对白气泡、情绪
- 底部固定按钮："一键生成全部图片"（当前点击提示"功能开发中"）

未实现（计划功能）：
- ~~每格可编辑所有字段：场景、角色、对白、镜头角度、情绪~~
- ~~每格有"AI 重写"按钮~~
- ~~每格有"单格生图"按钮~~

## 3.7 生图过程（未实现）

> **v3.1 状态**：后端 SSE 接口已实现（POST /api/generate-images），前端尚未对接。

计划功能：
- 留在脚本编辑页，每格卡片上直接显示生成的图片
- SSE 流式推送，每生成完一张立即显示
- 未完成的格子显示加载动画

## 3.8 生图完成后（未实现）

计划功能：
- 可修改文字（纯前端）
- 可点单格"重新生成"按钮（调后端）
- 底部出现"导出"按钮

## 3.9 导出（未实现）

计划功能：
- 单张分开导出（不拼合）
- 生成分享链接

## 3.9 探索页 — 详细组件

> **v3.1 状态**：UI 框架已实现，使用本地 mock 数据和 SVG 渐变色块代替真实图片。搜索和筛选为纯 UI 展示，未对接后端 API。

探索页为单页纵向滚动布局，左上角有悬浮返回按钮，从上到下依次排列：

### 区域 A：页面标题

| **组件** | **说明** |
| --- | --- |
| 标题 | "探索"（28px 加粗），左上角 |

### 区域 B：搜索栏

| **组件** | **说明** |
| --- | --- |
| 搜索框 | 圆角 12px，浅灰背景 #F5F5F5，左侧放大镜图标 |
| placeholder | "搜索作品、风格、创作者..." |
| 交互 | 当前仅为静态展示文本（非 TextInput），~~实时搜索~~（未实现） |

### 区域 C：风格筛选标签

| **组件** | **说明** |
| --- | --- |
| 布局 | 横向滚动，单行排列 |
| 标签内容 | 当前 5 个：全部 / 悬疑反转 / 搞笑沙雕 / 治愈温情 / 暗黑爆爽（计划增加：+ 古风仙侠 / 超能力奇幻 / 恐怖惊悚 / 甜宠恋爱） |
| 选中态 | 紫色背景 #5F33E1 + 白色文字 |
| 未选中态 | 白色背景 + 灰色边框 + 黑色文字 |
| 默认 | "全部"选中 |
| 交互 | 可点击切换选中态（前端 state），但未触发内容筛选 |

### 区域 D：作品瀑布流

| **组件** | **说明** |
| --- | --- |
| 布局 | 双列瀑布流（类小红书），列间距 12px |
| 数据 | 当前 4 张 mock 卡片（左列 2 张 + 右列 2 张），硬编码在前端 |
| 封面图 | 使用 SVG LinearGradient 渐变色块代替真实图片，不同卡片有不同渐变方向和配色 |

每张作品卡片结构：

| **元素** | **说明** |
| --- | --- |
| 封面图 | 卡片顶部，圆角 12px（上半），SVG 渐变色块，高度各异（140-220px） |
| 标题 | 封面下方，14px 黑色 Semibold，最多 2 行 |
| 作者名 | 标题下方左侧，11px 灰色 |
| 点赞数 | 标题下方右侧，爱心图标 + 数字，11px 灰色 |

点击卡片 → ~~进入作品详情页~~（未实现，当前无响应）。

## 3.10 我的页面 — 详细组件

> **v3.1 状态**：UI 框架已实现，所有数据为硬编码 mock（"创作者小明"、ID: 10086、统计数字等）。左上角有悬浮返回按钮。未对接后端 API。

我的页面为单页纵向布局，从上到下依次排列：

### 区域 A：页面标题栏

| **组件** | **说明** |
| --- | --- |
| 标题 | "我的"（28px 加粗），左上角 |
| 设置按钮 | 右上角，齿轮图标，~~点击进入设置页~~（当前无响应） |

### 区域 B：个人信息卡片

| **组件** | **说明** |
| --- | --- |
| 卡片样式 | 白色背景，圆角 16px，阴影投影 |
| 头像 | 左侧，紫色圆形背景 + 白色人形 SVG 图标，64px |
| 用户名 | 头像右侧，"创作者小明"（18px 加粗）— 硬编码 |
| 用户信息 | 用户名下方，"ID: 10086 · 新手创作者"（13px 灰色）— 硬编码 |
| 右箭头 | 卡片最右侧，> 图标，~~点击进入个人资料编辑页~~（未实现） |

### 区域 C：数据统计栏

| **组件** | **说明** |
| --- | --- |
| 布局 | 四等分横排，白色背景卡片，圆角 16px，阴影投影 |
| 数据项 | 作品 12 / 草稿 3 / 收藏 28 / 获赞 156 — 全部硬编码 |
| 数字 | 22px 加粗黑色 |
| 标签 | 数字下方，12px 灰色 |

### 区域 D：功能菜单列表

白色背景卡片，圆角 16px，每项之间有分割线。

> **v3.1 变更**：菜单从 5 项增加到 6 项，新增"待制作内容"（与后台任务系统联动）。

| **菜单项** | **图标** | **图标颜色** | **点击行为** |
| --- | --- | --- | --- |
| 待制作内容 | 时钟图标 | Warning #FF9500 | 有生成中任务时跳回加载页，否则查看最近完成的任务。显示 badge（"N 生成中" 或 "N 已完成"） |
| 我的作品 | 图片图标 | Primary #5F33E1 | ~~作品列表页~~（未实现） |
| 我的草稿 | 文档图标 | Warning #FF9500 | ~~草稿列表页~~（未实现） |
| 我的收藏 | 书签图标 | Success #34C759 | ~~收藏列表页~~（未实现） |
| 我的点赞 | 爱心图标 | Error #FF3B30 | ~~点赞列表页~~（未实现） |
| 使用记录 | 时钟图标 | Info #007AFF | ~~使用记录页~~（未实现） |

每项右侧显示 > 箭头（ChevronRight，#717171）。

---

# 4. 用户操作流程

## 4.1 核心流程：创作漫画

> **v3.1 变更**：3 步引导 + 后台任务系统

```
打开 App
  → 默认进入创作首页
  → 看到问候语 + 探索灵感卡片 + 故事输入区
  → 在输入框写故事（如"渣男被前女友报复"）
  → 点击「生成分镜脚本」按钮
  → 进入引导式创作页面（3 步）
    → 步骤 1/3: 选择时长（默认15秒）→ 点「下一步」
    → 步骤 2/3: 选择风格（如悬疑反转）→ 点「下一步」
    → 步骤 3/3: 选择镜头数（默认6）+ 图片比例（默认3:4）→ 点「开始生成」
  → 创建后台任务，进入加载页（4步进度动画）
    → 可选择「返回首页（后台生成）」→ 回首页，首页顶部出现紫色进度气泡
    → 可选择「取消生成」→ 任务标记为失败，返回首页
    → 等待完成后自动跳转结果页
  → 进入「脚本结果页」（只读展示）       ← 当前实现到此
    → 上下滑动查看每格分镜
    → 底部「一键生成全部图片」按钮（开发中）
  ------ 以下为计划功能，尚未实现 ------
  → 编辑脚本文字（纯前端）
  → AI 重写单格 / 单格生图
  → 一键生成全部图片（SSE 实时显示）
  → 导出 → 单张分开保存 + 生成分享链接
```

## 4.2 探索灵感流程

```
创作页「探索灵感」区域
  → 左右滑动浏览灵感卡片（mock 数据，Unsplash 图片）
  → 点击某张卡片 → （未实现，当前无响应）
  → 点击「查看全部」→ 跳转到探索页
```

## 4.3 探索发现流程

```
首页点击「查看全部」→ 进入探索页
  → 浏览瀑布流作品（mock 数据，SVG 渐变色块）
  → 搜索栏为静态展示（搜索功能未实现）
  → 可点击风格标签切换选中态（筛选功能未实现）
  → 点击作品卡片 → （未实现，当前无响应）
```

## 4.4 个人中心流程

```
首页点击头像 → 进入我的页面
  → 查看个人信息 + 数据统计（mock 数据）
  → 点击「待制作内容」→ 查看后台任务状态
  → 其他菜单项（我的作品/草稿/收藏/点赞/使用记录）→ 未实现
  → 设置按钮 → 未实现
```

---

# 5. 前后端技术交互流程

## 5.1 创作流程交互

```
┌─────────────┐                              ┌─────────────┐
│   前端 App   │                              │  后端 API   │
└──────┬──────┘                              └──────┬──────┘
       │                                            │
       │  1. 用户输入文案 + 选择参数                   │
       │  （纯前端，无请求）                           │
       │                                            │
       │  2. 点击「开始生成」                          │
       │──── POST /api/generate-script ────────────→│
       │     body: {                                │
       │       story: "渣男被前女友报复",              │
       │       style: "suspense_twist",             │
       │       duration: 15,                        │
       │       panel_count: 6,                      │
       │       aspect_ratio: "3:4"                  │
       │     }                                      │
       │                                            │
       │                          parse_story 节点   │
       │                          （调用通义千问）      │
       │                               ↓            │
       │                         refine_script 节点  │
       │                          （润色 + 生成       │
       │                           image_prompt）    │
       │                               ↓            │
       │←──── 返回完整脚本 JSON ─────────────────────│
       │     {panels: [{scene, dialogue,            │
       │       camera, emotion, image_prompt}, ...]} │
       │                                            │
       │  3. 展示脚本编辑页（纯前端编辑）               │
       │                                            │
       │  4. 点击「一键生成全部图片」                   │
       │──── POST /api/generate-images (SSE) ──────→│
       │     body: {panels: [...image_prompts]}     │
       │                                            │
       │                     对每格循环：              │
       │                     generate_image          │
       │                     （调用通义万相/Nano）      │
       │                          ↓                 │
       │                     上传 OSS                │
       │                          ↓                 │
       │←──── SSE: {panel_id: 1, image_url: ...} ──│
       │←──── SSE: {panel_id: 2, image_url: ...} ──│
       │←──── SSE: {panel_id: 3, image_url: ...} ──│
       │      ...逐张推送                             │
       │←──── SSE: done ───────────────────────────│
       │                                            │
       │  5. 用户编辑文字（纯前端，无请求）              │
       │                                            │
       │  6. 用户点击某格「重新生成」                   │
       │──── POST /api/regenerate-image ───────────→│
       │     body: {panel_id: 3, image_prompt: ...} │
       │←──── {image_url: "新图片地址"} ──────────────│
       │                                            │
       │  7. 点击「导出」                              │
       │──── POST /api/export ─────────────────────→│
       │     body: {work_id: "xxx"}                 │
       │←──── {images: [...urls],                   │
       │       share_link: "https://..."} ──────────│
       │                                            │
```

## 5.2 单格操作交互

| **用户操作** | **前端行为** | **后端接口** | **实现状态** |
| --- | --- | --- | --- |
| 编辑脚本文字 | 直接修改本地状态 | 无 | 未实现（当前只读） |
| AI 重写单格 | 显示加载 → 替换文字 | POST /api/rewrite-panel | 未实现（前后端均无） |
| 单格生图 | 显示加载 → 显示图片 | POST /api/generate-single-image | 后端已实现，前端未对接 |
| 重新生成图片 | 显示加载 → 替换图片 | POST /api/regenerate-image | 后端已实现，前端未对接 |

## 5.3 探索页交互（未实现）

> **v3.1 状态**：探索页当前使用前端硬编码 mock 数据，以下接口均未实现。

| **用户操作** | **前端行为** | **后端接口** | **说明** |
| --- | --- | --- | --- |
| 进入探索页 | 加载瀑布流 | GET /api/explore/recommend | 未实现 |
| 下拉刷新 | 重新请求 | GET /api/explore/recommend | 未实现 |
| 上滑加载更多 | 追加卡片 | GET /api/explore/recommend?page=N | 未实现 |
| 点击风格标签 | 切换内容 | GET /api/explore/category?style=X | 未实现 |
| 搜索关键词 | 显示搜索结果 | GET /api/explore/search?q=X | 未实现 |
| 点赞 | 爱心变红 + 数字+1 | POST /api/works/{id}/like | 未实现 |
| 收藏 | 书签高亮 | POST /api/works/{id}/collect | 未实现 |

## 5.4 我的页面交互（未实现）

> **v3.1 状态**：我的页面当前使用前端硬编码 mock 数据，以下接口均未实现。

| **用户操作** | **前端行为** | **后端接口** | **实现状态** |
| --- | --- | --- | --- |
| 进入我的页 | 显示 mock 数据 | GET /api/user/profile | 未实现 |
| 点击待制作内容 | 查看后台任务 | — | 已实现（前端 state） |
| 点击个人卡片 | 跳转编辑页 | — | 未实现 |
| 点击我的作品 | 加载作品列表 | GET /api/user/works | 未实现 |
| 点击我的草稿 | 加载草稿列表 | GET /api/user/drafts | 未实现 |
| 点击我的收藏 | 加载收藏列表 | GET /api/user/collections | V2 计划 |
| 点击我的点赞 | 加载点赞列表 | GET /api/user/likes | V2 计划 |
| 点击使用记录 | 加载记录列表 | GET /api/user/history | V2 计划 |

## 5.5 自动保存草稿（未实现）

```
计划功能：
  → 每 30 秒自动保存一次草稿
  → POST /api/drafts/save
  → 静默保存，不打断用户操作
```

---

# 6. 脚本生成规范

## 6.1 叙事黄金公式

所有脚本必须严格遵循 4 段式叙事结构：

| **阶段** | **名称** | **占比** | **作用** |
| --- | --- | --- | --- |
| 第 1 格 | Hook | 前 20% | 第一秒就抓住注意力，禁止铺垫 |
| 中间 | Escalate | 中间 50% | 快速推进，每格有新信息，情绪递增 |
| 倒数第 2 格 | Twist | 后 20% | 出人意料的反转，打破观众预期 |
| 最后 1 格 | Cliffhanger | 最后 10% | 留钩子，让观众想看下一集 |

## 6.2 禁止项

- 禁止第 1 格用于介绍背景或人物关系
- 禁止连续 2 格情绪强度相同
- 禁止最后一格给完整结局（必须留悬念）
- 禁止任何一格不含对白或旁白
- 禁止连续 2 格相同镜头角度

## 6.3 镜头数与叙事结构映射

| **镜头数** | **Hook (20%)** | **Escalate (50%)** | **Twist (20%)** | **Cliffhanger (10%)** |
| --- | --- | --- | --- | --- |
| 4 | 第 1 格 | 第 2 格 | 第 3 格 | 第 4 格 |
| 6 | 第 1 格 | 第 2-4 格 | 第 5 格 | 第 6 格 |
| 9 | 第 1-2 格 | 第 3-6 格 | 第 7-8 格 | 第 9 格 |
| 12 | 第 1-2 格 | 第 3-9 格 | 第 10-11 格 | 第 12 格 |

## 6.4 8 种脚本风格

| **编号** | **风格** | **英文标识** | **核心特点** |
| --- | --- | --- | --- |
| 1 | 悬疑反转 | suspense_twist | 每格埋伏笔，结尾大反转 |
| 2 | 搞笑沙雕 | comedy_absurd | 夸张表情，无厘头式反转 |
| 3 | 治愈温情 | healing_warm | 温柔细腻，情感反转 |
| 4 | 暗黑爆爽 | dark_revenge | 快节奏强冲突，复仇爽感 |
| 5 | 古风仙侠 | wuxia_fantasy | 古装场景，江湖气息 |
| 6 | 超能力奇幻 | superpower_fantasy | 科幻/魔法元素，觉醒成长 |
| 7 | 恐怖惊悚 | horror_thriller | 压抑氛围，惊吓结尾 |
| 8 | 甜宠恋爱 | sweet_romance | 快节奏对白，反转甜 |

## 6.5 时长选项

时长和镜头数独立，用户自由搭配。

| **时长** | **说明** |
| --- | --- |
| 10秒 | 超短篇，快节奏冲击 |
| 15秒（默认） | 标准漫剧节奏 |
| 30秒 | 中篇，剧情更完整 |
| 60秒 | 长篇，多线叙事 |
| ~~自定义~~ | ~~用户输入秒数~~（当前版本暂不支持） |

## 6.6 图片比例

| **比例** | **说明** |
| --- | --- |
| 3:4 竖版（默认） | 最适合手机漫剧 |
| 1:1 方形 | 适合社交媒体 |
| 16:9 横版 | 适合横屏展示 |
| ~~自定义~~ | ~~用户输入比例~~（当前版本暂不支持） |

## 6.7 缓存规则

相同文案 + 相同参数（风格、时长、镜头数）的脚本结果缓存 7 天，节省 API 调用成本。过期后重新生成。

---

# 7. 后端服务设计

## 7.1 LangGraph 状态图

### 链路 1：生成脚本（POST /api/generate-script）

parse_story → refine_script → 返回完整分镜脚本

**parse_story：** 接收文案 + 风格 + 时长 + 镜头数，调用通义千问，按黄金公式拆解分镜脚本

**refine_script：** 润色每格描述，补充视觉细节，为每格生成英文 image_prompt

### 链路 2：生成图片（POST /api/generate-images，SSE）

对每一格循环执行：generate_image → 上传 OSS → SSE 推送给 App

**generate_image：** 根据 image_prompt 调用通义万相或 Nano Banana 生图

MVP 不做风格一致性检查（check_style），V2 再加。

### 链路 3：单格操作（直接调用，不经过 LangGraph）

**rewrite-panel：** 直接调用通义千问重写单格脚本

**regenerate-image：** 直接调用生图 API 重新生成单格图片

**generate-single-image：** 为单格首次生成图片

## 7.2 API 接口完整清单

> **v3.1 更新**：新增"状态"列，标注每个接口的实现情况。

### MVP（12 个接口，已实现 4 个）

| **接口** | **方法** | **说明** | **状态** |
| --- | --- | --- | --- |
| /api/auth/login | POST | 登录（手机号/微信/Apple ID） | 未实现 |
| /api/auth/register | POST | 注册 | 未实现 |
| /api/generate-script | POST | 生成完整分镜脚本 | **已实现** |
| /api/rewrite-panel | POST | AI 重写单格脚本 | 未实现 |
| /api/generate-images | POST (SSE) | 一键生成全部图片，逐张推送 | **后端已实现**，前端未对接 |
| /api/generate-single-image | POST | 单格生图 | **后端已实现**，前端未对接 |
| /api/regenerate-image | POST | 重新生成指定格图片 | **后端已实现**，前端未对接 |
| /api/export | POST | 导出（单张分开 + 生成链接） | 未实现 |
| /api/user/works | GET | 我的作品列表 | 未实现 |
| /api/user/drafts | GET | 我的草稿列表 | 未实现 |
| /api/drafts/save | POST | 保存草稿 | 未实现 |
| /api/user/profile | GET | 个人资料 | 未实现 |

### V2（13 个接口，均未实现）

| **接口** | **方法** | **说明** | **状态** |
| --- | --- | --- | --- |
| /api/user/profile | PUT | 修改个人资料 | 未实现 |
| /api/explore/recommend | GET | 热门推荐作品 | 未实现 |
| /api/explore/latest | GET | 最新作品 | 未实现 |
| /api/explore/category | GET | 按风格分类 | 未实现 |
| /api/explore/search | GET | 搜索 | 未实现 |
| /api/works/{id}/like | POST | 点赞 | 未实现 |
| /api/works/{id}/collect | POST | 收藏 | 未实现 |
| /api/user/collections | GET | 我的收藏 | 未实现 |
| /api/user/likes | GET | 我的点赞 | 未实现 |
| /api/user/history | GET | 使用记录 | 未实现 |
| /api/user/works/{id} | DELETE | 删除作品 | 未实现 |
| /api/user/points | GET | 积分余额 | 未实现 |
| /api/points/consume | POST | 消耗积分 | 未实现 |

### V3（3 个接口，均未实现）

| **接口** | **方法** | **说明** | **状态** |
| --- | --- | --- | --- |
| /api/works/{id}/comment | POST | 评论 | 未实现 |
| /api/works/{id}/comments | GET | 获取评论列表 | 未实现 |
| /api/report | POST | 举报/反馈 | 未实现 |

---

# 8. 环境变量配置

| **变量名** | **用途** | **示例** |
| --- | --- | --- |
| LLM_API_KEY | 通义千问 API Key | sk-xxxxx |
| LLM_BASE_URL | 通义千问 API 地址 | https://dashscope.aliyuncs.com/compatible-mode/v1 |
| LLM_MODEL | 文本模型 | qwen-plus |
| IMAGE_API_KEY | 通义万相 API Key | sk-xxxxx |
| IMAGE_BASE_URL | 通义万相 API 地址 | https://dashscope.aliyuncs.com/api/v1/... |
| IMAGE_MODEL | 生图模型 | wan2.5-t2i |
| GOOGLE_API_KEY | Gemini API Key（Nano Banana） | 可选 |
| OSS_ACCESS_KEY | 阿里云 OSS Access Key |  |
| OSS_SECRET_KEY | 阿里云 OSS Secret Key |  |
| OSS_BUCKET | OSS Bucket 名称 | comic-storyboard |
| DB_URL | MySQL 连接地址 | mysql://user:pass@host/db |

- API Key 不得提交到 Git，确保 .env 在 .gitignore 中
- 未配置 API Key 时后端返回 mock 数据

---

# 9. 开发路线图

## 9.1 第 1 周：后端核心 + 脚本生成 — **已完成**

- [x] 初始化 FastAPI + LangGraph 项目
- [x] 实现 parse_story + refine_script 节点
- [x] 实现 generate-script API（含 mock 回退）
- [x] curl 测试跑通

## 9.2 第 2 周：生图 + 存储 — **部分完成**

- [x] 对接通义万相 API（image_service.py）
- [ ] 对接阿里云 OSS（未实现）
- [x] 实现 generate-images SSE 接口（后端）
- [x] 实现单格生图 + 重新生成（后端）

## 9.3 第 3 周：用户系统 + 数据库 — **未开始**

- [ ] MySQL 表结构设计
- [ ] 登录/注册 API
- [ ] 我的作品 + 草稿 API
- [ ] 导出 API

## 9.4 第 4 周：React Native 前端 — **部分完成**

- [x] 初始化 React Native (Expo) 项目
- [x] 实现创作页（问候栏 + 探索灵感 + 故事输入 + 3步引导式创作流程）
- [x] 实现探索页 UI（搜索 + 筛选 + 瀑布流）— mock 数据
- [x] 实现我的页面 UI（个人卡片 + 统计 + 6项菜单列表）— mock 数据
- [x] 实现脚本结果页（只读展示）
- [x] 实现加载页 + 后台任务系统
- [ ] 前端对接 SSE 生图实时显示
- [ ] 脚本编辑功能（文字编辑、AI 重写、单格生图）
- [ ] 底部 Tab 栏
- [ ] 页面转场动画

## 9.5 第 5 周：联调 + 优化 + 上架 — **未开始**

- [ ] 前后端联调
- [ ] iOS 体验优化（触感反馈、动画、手势）
- [ ] 测试、修 bug
- [ ] App Store 审核提交

---

# 10. 当前实现状态总览

> **v3.1 新增**：快速参考表，一目了然。

| **模块** | **状态** | **说明** |
| --- | --- | --- |
| **前端 — 创作页** | **已完成** | 问候栏、灵感卡片、故事输入、生成按钮 |
| **前端 — 3步引导** | **已完成** | 时长→风格→镜头/比例，圆点进度指示器 |
| **前端 — 加载页** | **已完成** | 4步进度动画、后台生成、取消生成 |
| **前端 — 后台任务** | **已完成** | 进度气泡、完成气泡、待制作内容菜单联动 |
| **前端 — 脚本结果页** | **部分完成** | 只读展示（叙事阶段、镜头角度、对白等），不可编辑 |
| **前端 — 探索页** | **UI 完成** | 搜索栏、标签筛选、瀑布流，全部 mock 数据 |
| **前端 — 我的页面** | **UI 完成** | 个人卡片、统计、6项菜单，全部 mock 数据 |
| **前端 — 生图展示** | 未实现 | 前端未对接 SSE 生图 |
| **前端 — 脚本编辑** | 未实现 | 文字编辑、AI 重写、单格生图 |
| **前端 — 导出** | 未实现 | |
| **前端 — 登录** | 未实现 | |
| **前端 — 底部 Tab 栏** | 未实现 | 当前用 screen state 切换 |
| **后端 — 脚本生成** | **已完成** | LangGraph 两节点 + generate-script API |
| **后端 — 生图 SSE** | **已完成** | generate-images 流式推送 |
| **后端 — 单格生图** | **已完成** | generate-single-image + regenerate-image |
| **后端 — 重写脚本** | 未实现 | rewrite-panel API |
| **后端 — 用户系统** | 未实现 | 登录/注册/个人资料 |
| **后端 — 数据库** | 未实现 | MySQL 表结构 |
| **后端 — OSS 存储** | 未实现 | 阿里云 OSS 对接 |
| **后端 — 导出** | 未实现 | export API |
| **设计 — 设计稿** | **已完成** | Pencil .pen 文件 + 导出截图 |
| **设计 — 主题系统** | **已完成** | theme.ts（Colors/Radius/Fonts/Spacing/Shadows） |
| **设计 — 图标** | **已完成** | Lucide SVG 图标组件（12个） |

---

# 11. 确认汇总表

| **决策项** | **最终结果** |
| --- | --- |
| 前端技术 | React Native + Expo（iOS + 网页 + 未来 Android） |
| 后端框架 | Python FastAPI + LangGraph |
| 数据库 | MySQL（未实现） |
| 图片存储 | 阿里云 OSS（未实现） |
| 部署 | 阿里云 ECS（2核4G）（未部署） |
| 文本 AI | 通义千问 qwen-plus |
| 生图 AI | 通义万相 wan2.5-t2i + Nano Banana |
| 登录方式 | 计划：手机号 / 微信 / Apple ID（未实现） |
| 页面导航 | 当前：Screen state 切换（计划：底部 Tab 栏） |
| 创作页布局 | 问候栏 → 探索灵感 → 故事输入 → 生成按钮 |
| 创作流程 | 首页输入文案 → 3 步引导（时长→风格→镜头数/比例）→ 后台生成 |
| 探索页布局 | 标题 → 搜索栏 → 5个风格标签 → 瀑布流双列卡片（mock） |
| 我的页布局 | 标题+设置 → 个人卡片 → 数据统计 → 6项菜单（含待制作内容） |
| 时长选项 | 10s / 15s / 30s / 60s |
| 风格 | 8 种（悬疑/搞笑/治愈/暗黑/仙侠/奇幻/恐怖/甜宠） |
| 镜头数 | 4 / 6 / 9 / 12，默认 6 |
| 图片比例 | 3:4 / 1:1 / 16:9，默认 3:4 |
| 叙事结构 | Hook → Escalate → Twist → Cliffhanger |
| 生图方式 | SSE 流式推送（后端已实现，前端未对接） |
| 设计规范 | 灵镜 Design System v1.0，主色 Primary #5F33E1，浅色主题 |
| 图标规范 | Lucide SVG 图标组件，选中态紫色 |
| MVP 接口数 | 12 个（已实现 4 个） |
| V2 接口数 | 13 个（均未实现） |
| V3 接口数 | 3 个（均未实现） |
